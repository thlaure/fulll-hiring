default:
    autoload:
        '': '%paths.base%/bootstrap'
    suites:
        default:
            paths:
                - '%paths.base%/../features'
            contexts:
                - FeatureContext
        register_vehicle:
            paths:
                - '%paths.base%/../features'
            contexts:
                - RegisterVehicleContext